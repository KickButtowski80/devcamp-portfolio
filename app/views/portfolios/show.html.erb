<div class="container portfolio-container">
 <div class="row" style="padding-top: 30px">
  <div id="protfolio-img" class="col-md-7 col-sm-8" >
   <%= image_tag portfolio_img(@portfolio_item.main_image, "main") unless @portfolio_item.main_image.nil?%>
  </div>
  <div class="col-md-5 col-sm-4">
     <%= @portfolio_item.title %> 
     <hr>
     <%= @portfolio_item.subtitle %> 
     <%= @portfolio_item.body %> 
     <hr>
     <h3>Technologies Used:</h3>  
     <% @portfolio_item.technologies.each do |t| %>
      <p><%= t.name %></p>
     <%end%>
  </div>
 </div>
</div> 

 
  

<%#= params.inspect %>
 
<%#= session.inspect %>
 
     